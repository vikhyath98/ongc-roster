import ModulePlaceholder from '../components/ModulePlaceholder'

export default function Reserve() {
  return (
    <ModulePlaceholder title="Reserve pool" spec="§5.6, §3.4, §6.3, §6.6">
      Base staff that pass eligibility, are cert-current, and have a live
      (unexpired) confirmation — the “ready to deploy” list.
    </ModulePlaceholder>
  )
}
